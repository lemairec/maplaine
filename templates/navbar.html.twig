<nav class="navbar navbar-expand-lg navbar-dark fixed-top bg-dark  mb-4" style="padding-top: 0;padding-bottom: 0; min-height: 60px">

    <div class="container">
        <a class="navbar-brand" href="{{ url('home') }}" style="padding-top: 0;padding-bottom: 0;"><img src="/images/beta.jpg" alt=""height="60" width="60">
Ma Plaine</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{'Admin' | trans}}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('admin_users') }}">Users</a>
                            <a class="dropdown-item" href="{{ url('admin_companies') }}">Companies</a>
                            <a class="dropdown-item" href="{{ url('admin_metacultures') }}">Cultures Admin</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('robots') }}">Robots</a>
                            <a class="dropdown-item" href="{{ url('robot_jobs') }}">Jobs</a>
                            <div class="dropdown-divider"></div>

                            <a class="dropdown-item" href="{{ url('silo_balises') }}">Silo balises</a>
                            <a class="dropdown-item" href="{{ url('silo_moteurs') }}">Silo moteurs</a>
                            <a class="dropdown-item" href="{{ url('silo_sechoir') }}">Silo sechoir</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('job_gpss_me') }}">GPS Job</a>
                            <a class="dropdown-item" href="{{ url('gps_parcelles') }}">GPS Parcelle</a>
                            <a class="dropdown-item" href="{{ url('gps_balises') }}">GPS Balises</a>
                            <div class="dropdown-divider"></div>
                            {% if app.user.username == "lejard" %}
                            <a class="dropdown-item" href="{{ url('annonces') }}">Annonces</a>
                            <a class="dropdown-item" href="{{ url('annonces_immobilier') }}">Immobilier</a>
                            {% endif %}

                        </div>
                    </li>
                    {% endif %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Société
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('campagnes') }}">Campagnes</a>
                            <a class="dropdown-item" href="{{ url('ilots') }}">Ilots</a>
                            <a class="dropdown-item" href="{{ url('cultures') }}">Cultures</a>
                            {% if app.user.username == "lejard" %}
                            <a class="dropdown-item" href="{{ url('analyse_sols') }}">Analyses Sol</a>
                            {% endif %}
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('materiels') }}">Materiel</a>
                            <a class="dropdown-item" href="{{ url('materiels_travail') }}">Travaux matériel</a>
                            {% if app.user.username == "lejard" %}
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('documents') }}">Documents</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('import_pac') }}">Import Pac</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('gasoils') }}">Gasoil</a>
                            <a class="dropdown-item" href="{{ url('deplacements') }}">Deplacements</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('appartement_operations') }}">Appartement Opérations</a>
                            <a class="dropdown-item" href="{{ url('appartement_bilan') }}">Bilan</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('apiculture') }}">Ruches</a>
                            {% endif %}

                        </div>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="{{ url('parcelles_map') }}">Parcelles</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url('interventions') }}">Interventions</a></li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Produits
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('produits') }}">Produits</a>
                            <a class="dropdown-item" href="{{ url('achats') }}">Achats</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('stocks') }}">Stocks</a>
                            {% if app.user.username == "lejard" %}
                            <a class="dropdown-item" href="{{ url('produit_campagnes') }}">Prix campagne</a>
                            {% endif %}
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('ephy_produits') }}">Ephy</a>
                        </div>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Bilan
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">


                            <a class="dropdown-item" href="{{ url('fiches_parcellaires') }}">Fiches Parcellaires</a>
                            {% if is_granted('ROLE_NEW_FEATURE') %}
                            <a class="dropdown-item" href="{{ url('bilan_intervention_prix') }}">Bilan intervention</a>
                            <a class="dropdown-item" href="{{ url('bilan_charges') }}">Bilan charges</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('bilan') }}">Bilan</a>
                            <a class="dropdown-item" href="{{ url('bilan_engrais') }}">Bilan engrais</a>
                            <a class="dropdown-item" href="{{ url('bilan_rendements') }}">Rendements</a>
                            {% if app.user.username == "lejard" %}
                            <a class="dropdown-item" href="{{ url('bilan_produits') }}">Produits</a>
                            <a class="dropdown-item" href="{{ url('bilan_comptes') }}">Comptes</a>
                            <a class="dropdown-item" href="{{ url('bilan_gasoil') }}">Gasoil</a>
                            <a class="dropdown-item" href="{{ url('bilan_dates') }}">Dates</a>
                            <a class="dropdown-item" href="{{ url('parcelles_ppf') }}">Plan de fumure</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('engrais') }}">Bilan coût engrais</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('calendar') }}">Calendrier</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('parcelles_ppf') }}">Ppf</a>
                            <a class="dropdown-item" href="{{ url('parcelles_grele') }}">Grele</a>
                            {% endif %}
                            {% endif %}

                        </div>
                    </li>

                    {% if is_granted('ROLE_NEW_FEATURE') %}

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Gestion
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('comptes') }}">Comptes</a>
                            <a class="dropdown-item" href="{{ url('comptes_by_year') }}">Comptes by Year</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('operations') }}">Operations</a>
                            <a class="dropdown-item" href="{{ url('factures_fournisseurs') }}">Factures</a>
                            <a class="dropdown-item" href="{{ url('emprunts') }}">Emprunts</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('commercialisations') }}">Commercialisations</a>
                            <a class="dropdown-item" href="{{ url('bilan_commercialisations') }}">Bilan Commercialisations</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('phytos') }}">Bilan Phytos</a>
                            {% if app.user.username == "lejard" %}
                            <a class="dropdown-item" href="{{ url('bilan_banque') }}">Bilan Banque</a>
                            <a class="dropdown-item" href="{{ url('bilan_emprunt') }}">Bilan Emprunt</a>
                            {% endif %}

                        </div>
                    </li>
                    {% endif %}
                    {% if is_granted('ROLE_COTATION') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Cotations
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('cotation_home') }}">Home</a>
                            <a class="dropdown-item" href="{{ url('cotations') }}">Cotations</a>
                            <a class="dropdown-item" href="{{ url('prix_moyens') }}">Prix Moyens</a>
                            <a class="dropdown-item" href="{{ url('cotation_produits') }}">Produits</a>
                            <a class="dropdown-item" href="/cotation/bilan/2024">Bilan 24</a>
                            <a class="dropdown-item" href="/cotation/bilan/2025">Bilan 25</a>
                            
                        </div>
                    </li>
                            
                    {% endif %}

                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <!--button(type='button', class='btn btn-default navbar-btn') Sign in-->
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Profile
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item"  href="{{ url('fos_user_security_logout') }}">Deconnexion</a>
                        <a class="dropdown-item"  href="{{ url('fos_user_profile_edit') }}">Mon Profil</a>
                        <a class="dropdown-item"  href="{{ url('fos_user_change_password') }}">Changer mon mot de passe</a>
                        {% if app.user.username == "lejard" %}
                        <a class="dropdown-item"  href="{{ url('alertes') }}">Alertes</a>
                        <a class="dropdown-item"  href="{{ url('export') }}">Export</a>
                        {% endif %}
                    </div>
                </li>
                {% else %}
                <li><button type="button" class="btn btn-light navbar-btn ab-rounded m-1" onclick="location.href='{{ url('fos_user_security_login') }}'">Se connecter</button></li>
                <li><button type="button" class="btn btn-outline-light navbar-btn ab-rounded m-1" onclick="location.href='{{ url('fos_user_registration_register') }}'">S'inscrire</button></li>
                {% endif %}
            </ul>

        </div>

    </div>
</nav>
